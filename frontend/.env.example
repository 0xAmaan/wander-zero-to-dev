# API Configuration
# Client-side API URL (used by browser)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Server-side API URL (used by Next.js SSR)
# In local dev, use localhost. In K8s, use the service name.
API_URL_INTERNAL=http://localhost:8080
